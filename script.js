function decimalToBinary(num) {
 arr.sort();
	let n=arr.length;
	for(let i=0;i<n-1;i++){
		const vat = Math.abs(arr[0]-arr[1]);
		if(vat==1){
			arr.shift();
		}
	}
	if(arr.length==1){
		return "YES";
	}
	else{
		return "NO";//Write you code here
  
}

